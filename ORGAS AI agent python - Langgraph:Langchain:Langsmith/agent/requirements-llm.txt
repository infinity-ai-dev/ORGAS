# LangGraph + LangChain Core (ESSENCIAL)
langgraph>=0.0.60
langchain>=0.1.5
langchain-core>=0.1.20
langchain-community>=0.0.20

# LLM Providers com SDKs de Desenvolvimento

# Google Gemini (Principal)
google-generativeai>=0.3.0
langchain-google-genai>=0.0.1

# OpenAI (Fallback)
openai>=1.3.0
langchain-openai>=0.0.1

# Anthropic (Fallback)
anthropic>=0.7.0
langchain-anthropic>=0.1.0

# Grok / xAI (Fallback)
# Nota: xAI usa OpenAI-compatible API, mas incluindo SDK direto
grok-api>=0.1.0  # SDK Grok quando disponível

# Async HTTP para chamadas diretas
aiohttp>=3.13.0
httpx>=0.25.0
